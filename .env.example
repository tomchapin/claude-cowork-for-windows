# Claude macOS Sandbox Configuration
# Copy this file to .env and customize as needed

# Display resolution
WIDTH=1920
HEIGHT=1080

# Resource allocation
RAM_SIZE=8
CPU_CORES=4

# Port mappings (change if defaults conflict with other services)
VNC_PORT=5999
SSH_PORT=50922
